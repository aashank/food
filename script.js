// function handleScrollAction(id) {
//     let element = document.getElementById(id)
//     if (element) {
//         element.scrollIntoView({
//             behavior: 'smooth'
//         })
//     }

// }

function handleReloadAction(id) {
    if (id === 'home')
        window.location.reload();
}